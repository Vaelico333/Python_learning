# Question: Please try to guess what is missing in the following code and add the missing part so that the code works fine.
'''import math
print(math.pow(2))'''

# My answer:
# It requires two arguments: one for the base and one que the power
import math
print(math.pow(2,3))